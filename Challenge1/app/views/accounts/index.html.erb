<nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top" style="padding-top: 5px; padding-bottom: 5px;">
  <div class="container">
    <span class="navbar-brand" style="font-size: 2em; font-weight: bold; margin-right: 20px;">Contas</span>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item <%= 'active' if current_page?(root_path) %>">
          <%= link_to 'Página Inicial', root_path, class: 'nav-link' %>
        </li>
        <li class="nav-item <%= 'active' if current_page?(new_account_path) %>">
          <%= link_to 'Nova Conta', new_account_path, class: 'nav-link' %>
        </li>
        <li class="nav-item <%= 'active' if current_page?(filtrar_contas_path) %>">
          <%= link_to 'Filtrar Contas', filtrar_contas_path, class: 'nav-link' %>
        </ul>
    </div>
  </div>
</nav>

<div class="text-center mt-3">
  <p class="alert "><%= notice %></p>
 
  <div id="accounts" class="mx-auto">
    <% @accounts.each do |account| %>
      <br>
      <%= render account %>
      <p>
        <%= link_to "Mostrar esta conta", account, class: "btn btn-info btn-sm" %>
      </p>
    <% end %>
  </div>
</div>
